{
  "name": "ion-slider-sorting-bug",
  "private": true,
  "version": "0.0.1",
  "description": "ion-slider-sorting-bug",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch"
  },
  "dependencies": {
    "@ionic/core": "4.6.0"
  },
  "devDependencies": {
    "@stencil/core": "1.1.2"
  }
}
